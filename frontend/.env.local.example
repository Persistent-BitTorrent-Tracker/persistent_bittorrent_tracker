# ── Backend API URL ────────────────────────────────────────────────────────────
# URL of the PBTS backend server. Must be prefixed with NEXT_PUBLIC_ so that
# Next.js bundles it into the browser build.
NEXT_PUBLIC_BACKEND_URL=http://localhost:3001

# ── Chain configuration ────────────────────────────────────────────────────────
# Chain ID for the network your ReputationTracker is deployed on.
#   Ethereum Sepolia : 11155111
#   Avalanche Fuji   : 43113
NEXT_PUBLIC_CHAIN_ID=11155111

# ── Contract addresses (filled after deployment) ───────────────────────────────
# Address of the deployed ReputationTracker contract.
NEXT_PUBLIC_REPUTATION_TRACKER_ADDRESS=
